export class User {
    gid: number;
    token: string;
    id: number;
    email: string;
    name: string;
    verified: boolean;

    // constructor();
    // constructor(userInfo: any);
    // constructor(userInfo?: any) {
    //     this.gid = userInfo && userInfo.gid || null;
    //     this.token = userInfo && userInfo.token || null;
    //     this.id = userInfo && userInfo.id || null;
    //     this.email = userInfo && userInfo.email || '';
    //     this.name = userInfo && userInfo.name || '';
    //     this.verified = userInfo && userInfo.verified || false;
    // }
    //
    // private calculateRating(userInfo) {
    //     //TODO RATINGS CALCULATIONS
    // }
    //
    // save() {
    //     //TODO SAVING ANY UPDATES
    // }
}